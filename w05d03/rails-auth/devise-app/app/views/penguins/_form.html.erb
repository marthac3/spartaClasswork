<%= form_with(model: penguin, local: true) do |form| %>
  <% if penguin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(penguin.errors.count, "error") %> prohibited this penguin from being saved:</h2>

      <ul>
      <% penguin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :penguin_name %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.number_field :height, id: :penguin_height %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight, id: :penguin_weight %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :penguin_color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
